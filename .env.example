# Temporary directory for file processing (optional, defaults to system temp)
TEMP_DIR=

# Maximum file size in MB (default: 500)
MAX_FILE_SIZE_MB=500

# FFmpeg command timeout in seconds (default: 300)
FFMPEG_TIMEOUT_SECONDS=300
